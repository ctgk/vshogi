# Contribution Guidelines

## Setup

```shell
git clone git@github.com:ctgk/vshogi.git
pip install nanobind scikit-build-core[pyproject]
pip install --no-build-isolation -ve .[dlshogi,develop]
pre-commit install
```
